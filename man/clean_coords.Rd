% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_coords.R
\name{clean_coords}
\alias{clean_coords}
\title{Clean geographic coordinates in a data frame}
\usage{
clean_coords(
  x,
  species_column = "species",
  remove_capitals = TRUE,
  remove_centroids = TRUE,
  remove_duplicates = TRUE,
  remove_gbif = TRUE,
  remove_institutions = TRUE,
  remove_invalid = TRUE,
  remove_hotspots = TRUE,
  remove_outliers = TRUE,
  remove_sea = TRUE,
  remove_urban = TRUE,
  remove_zero = TRUE,
  outlier_settings = NULL,
  round_settings = NULL
)
}
\arguments{
\item{x}{A data frame containing geographic data, including coordinates and species information.}

\item{species_column}{A character string specifying the name of the column containing species names. Default is `"species"`.}

\item{remove_capitals}{Logical. If `TRUE`, removes records located in capital cities. Default is `TRUE`.}

\item{remove_centroids}{Logical. If `TRUE`, removes records located in country centroids. Default is `TRUE`.}

\item{remove_duplicates}{Logical. If `TRUE`, removes duplicate records for the same species. Default is `TRUE`.}

\item{remove_gbif}{Logical. If `TRUE`, removes records associated with the GBIF headquarters. Default is `TRUE`.}

\item{remove_institutions}{Logical. If `TRUE`, removes records associated with institutions (e.g., herbaria). Default is `TRUE`.}

\item{remove_invalid}{Logical. If `TRUE`, removes invalid geographic coordinates. Default is `TRUE`.}

\item{remove_hotspots}{Logical. If `TRUE`, removes records associated with biodiversity hotspots such as airports or harbors. Default is `TRUE`.}

\item{remove_outliers}{Logical. If `TRUE`, removes spatial outliers based on the species' distribution. Default is `TRUE`.}

\item{remove_sea}{Logical. If `TRUE`, removes records located in the sea. Default is `TRUE`.}

\item{remove_urban}{Logical. If `TRUE`, removes records located in urban areas. Default is `TRUE`.}

\item{remove_zero}{Logical. If `TRUE`, removes records with zero latitude or longitude. Default is `TRUE`.}

\item{outlier_settings}{A list of additional settings for outlier detection, passed to `CoordinateCleaner::cc_outl`.}

\item{round_settings}{A list of additional settings. If not `NULL`, these settings are passed to `CoordinateCleaner::cd_round`.
Default is `NULL`, which uses standard parameters (`tdi = 1000`, `method = "distance"`, `min_occs = 4`).}
}
\value{
A cleaned data frame with problematic records removed based on the specified criteria.
}
\description{
This function uses the `CoordinateCleaner` package to clean and filter geographic coordinates
in a data frame, removing problematic records such as duplicates, outliers, invalid points,
and points located in capitals, centroids, or urban areas.
}
\details{
- This function sequentially applies a series of cleaning functions from the `CoordinateCleaner` package.
- Each cleaning step can be enabled or disabled using its corresponding parameter.
- Outlier detection (`cc_outl`) uses customizable parameters via the `outlier_settings` argument.
- Records with `individualCount == 0` or `occurrenceStatus == "ABSENT"` are also removed.
}
\examples{
# Example: Clean a dataset of species observations
# observations <- get_observations("Tragopogon dubius")
clean_data <- clean_coords(
  x = observations,
  species_column = "species_name",
  remove_outliers = TRUE,
  remove_sea = FALSE
)

# Example: Customize outlier detection settings
clean_data <- clean_coords(
  x = observations,
  outlier_settings = list(tdi = 500, method = "quantile")
)

}
